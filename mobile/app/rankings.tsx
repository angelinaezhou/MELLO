import { View, Text } from "react-native";

export default function Rankings() {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text style={{ fontSize: 18 }}>Your ranked songs (1–100)</Text>
    </View>
  );
}